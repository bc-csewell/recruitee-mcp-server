# Base deployment URL for the server (required for OAuth2 and document access). Example: https://your-app.fly.dev
# FastMCP uses this to construct OAuth2 redirect URIs: {BASE_DEPLOY_URL}/auth/callback
BASE_DEPLOY_URL=

# Your Recruitee company ID - find this in your Recruitee account
RECRUITEE_COMPANY_ID=
# Your Recruitee API token - generate this in Recruitee > Settings > API
RECRUITEE_API_TOKEN=

# ===== Google OAuth2 Configuration (FastMCP OAuth Proxy) - REQUIRED =====
# OAuth2 is required for /mcp authentication
# FastMCP automatically creates /authorize, /token, and /auth/callback endpoints

# Get these from your Google Cloud Console OAuth2 credentials:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application type)
# 3. Add authorized redirect URI: {BASE_DEPLOY_URL}/auth/callback
#    Example: https://recruitee-mcp-server-vfcymcigxa-nw.a.run.app/auth/callback

# OAuth2 Client ID from Google Cloud Console
GOOGLE_OAUTH_CLIENT_ID=

# OAuth2 Client Secret from Google Cloud Console
GOOGLE_OAUTH_CLIENT_SECRET=

# Optional: Restrict access to a specific Google Workspace domain
# Example: buildcircle.co.uk (only users with @buildcircle.co.uk emails can access)
ALLOWED_OAUTH_DOMAIN=

# ===== End OAuth2 Configuration =====

# Username and Password for document access login form
DOCUMENTS_USERNAME=
DOCUMENTS_PASSWORD=
# Secret token for document access authentication. This is what gets stored in the auth cookie after successful login
DOCUMENTS_TOKEN=
# Directory for static file storage and serving via /documents endpoint. Should match [mounts] destination if using fly.io for deployments, or use e.g. "./data" for local dev# Directory for static file storage and serving via /documents endpoint. Should match [mounts] destination if using fly.io for deployments, or use e.g. "./data" for local dev
DOCUMENTS_DIR=/data